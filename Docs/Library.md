# Library Documentation
## BetterDict
BetterDict is a small(yah really small ngl lol) library that greatly enhance user experience by supporting user to access a Dictionary Value by Dictionary["Key"] and Dictionary.Key (Object and Dictionary method)

With this library, we can now use Object.PinName.Value instead of Object["PinName"].Value